\section{A Formal Framework for Verbal Reinforcement Learning}
\label{sec:framework}

% WRITING BRIEF (delete when drafted). Fixes reviewer weaknesses (a), (b), (d).
% 1. Setting: language-augmented POMDP. Define where a linguistic artifact ell can enter:
%    goal/task specification, observation/state, action prior, reward (specification-time
%    construction vs runtime judgment), transition prior, and the improvement operator
%    (inference-time revision, memory write, parameter update).
% 2. Definitional fence (the two criteria used in the ARR author responses, keep wording aligned):
%    (i) linguistic structure functionally consumed, not collapsed to a scalar/preference bit
%        before use; (ii) participates in an improvement loop (revision, memory write, or
%        parameter update).
%    Exclusions: plain instruction following (no loop); vanilla RLHF/DPO (scalarized before
%    learning; shown only as the compression-spectrum endpoint). Boundary clarification:
%    specification-time consumption (reward-code generation) satisfies (i).
% 3. Terminology genealogy: Shinn et al. 2023 coined the term for self-reflection
%    (subtitle: Language Agents with Verbal Reinforcement Learning); we generalize; relation
%    to NLRL (Feng et al., 2411.14251: language Bellman operators, language value functions),
%    to feedback-conditional policies (2509.22638), to memory-augmented MDPs (AgentFly
%    2508.16153), and to psychology's older "verbal reinforcement".
% 4. Positioning: LLM-era successor to Luketina et al. 2019 (language-conditional vs
%    language-assisted RL); CoALA organizes computation, we organize signals.
% 5. State plainly which formal guarantees exist and which do not (feeds the agenda in S13).

\todonote{Draft from T1 notes + NLRL deep-read.}
