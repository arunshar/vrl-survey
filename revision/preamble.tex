% Shared preamble for the marked (main.tex) and clean (main_clean.tex) builds.
\usepackage{times}
\usepackage{latexsym}
\usepackage{tikz}
\usepackage{hyperref}
\usepackage[edges]{forest}
\usepackage{graphicx}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\algnewcommand{\LeftComment}[1]{\Statex{}$\triangleright$~#1}
\usepackage{multicol}
\usepackage{rotating}
\usetikzlibrary{trees,shapes,calc,arrows.meta,positioning}
\usepackage{makecell}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{colortbl}
\usepackage{adjustbox}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{pifont}
\usepackage{array}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{xspace}

\definecolor{branch1}{HTML}{2E86C1}
\definecolor{branch2}{HTML}{27AE60}
\definecolor{branch3}{HTML}{E67E22}
\definecolor{branch4}{HTML}{8E44AD}
\definecolor{branch5}{HTML}{C0392B}
\definecolor{leafbg}{HTML}{F0F4F8}

\setlength{\textfloatsep}{5pt}
\setlength{\floatsep}{5pt}
\setlength{\intextsep}{5pt}
\setlength{\emergencystretch}{3em}
\setcounter{dbltopnumber}{2}
\renewcommand{\dbltopfraction}{0.95}
\renewcommand{\dblfloatpagefraction}{0.9}
\renewcommand{\textfraction}{0.05}
\newcommand{\das}[1]{\textcolor{orange}{das: #1}}

\raggedbottom%
\hbadness=10000
\vbadness=10000
\newcommand{\verbalrl}{Verbal\ RL\xspace}

% ---- Revision markup (direct-replacement policy) ---------------------------
% \added{...}   : new or updated text (red in the marked build, plain in clean)
% \removed{...} : replaced text; renders in NEITHER build. Changes are shown by
%                 direct replacement: the superseded text is dropped and the
%                 replacement appears in red via \added. The source keeps the
%                 old text inside \removed{} for provenance.
% The \ifmarkup switch is set in main.tex (true) / main_clean.tex (false).
\newif\ifmarkup
\newcommand{\added}[1]{\ifmarkup\textcolor{red}{#1}\else#1\fi}
\newcommand{\removed}[1]{}
% ----------------------------------------------------------------------------

% ---- Two-column float safety around \textwidth (table*/figure*) -----------
% The [review] build loads the `lineno` package for line numbers. lineno's own
% documentation warns that its twocolumn-tracking machinery can corrupt the
% placement of full-width (table*/figure*) floats unless line numbering is
% explicitly suspended around them, and this failure mode is known to vary
% by TeX engine/distribution. \linenumbers only exists when lineno is loaded
% (review build); \nolinenumbers is a safe no-op in the clean build. These
% wrappers apply the suspend/resume only when markup mode (review) is active.
\newcommand{\widefloatnonumbers}{\ifmarkup\nolinenumbers\fi}
\newcommand{\widefloatresumenumbers}{\ifmarkup\linenumbers\fi}
% ----------------------------------------------------------------------------

\title{The Rise of Verbal Reinforcement Learning: A Survey}
\author{Anonymous ACL Submission}
